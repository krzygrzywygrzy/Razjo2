import 'package:flutter/material.dart';

class DashboardMessagesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Notifications"),
    );
  }
}
